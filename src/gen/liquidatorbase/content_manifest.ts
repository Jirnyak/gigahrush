/* ── Реестр контента Базы Ликвидаторов ────────────────────────────
 *
 * Точка сборки того, что регистрируется НА ИМПОРТЕ и своего генератора не
 * имеет. Соседние этажи держат здесь же и вызов POI-генераторов
 * (`runMinistryContent` и подобные); у Базы таких пока нет, и выдуманного
 * пустого раннера тут не будет — он был бы мёртвым кодом.
 *
 * Комнату-якорь сцены роет геометрия форта (`fort.ts`), поэтому сцена
 * подключается голым импортом — второй из двух путей `cutscene.md` §9.
 */

import './arena_duel';
