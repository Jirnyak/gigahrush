/* ── Договор стартового сиквенса ───────────────────────────────────
 *
 * Первые минуты игры: игрок стоит в запертой Столовой, пьёт из раковины, идёт в
 * Уборную, поднимает там ключ и отпирает им дверь в Актовый зал, где его принимает
 * Ольга Дмитриевна.
 *
 * Шаг сиквенса НЕ ХРАНИТСЯ. Он выводится из состояния мира — есть ли нужда, есть
 * ли ключ в сумке (`systems/target_guide.ts`), — и потому не может разъехаться с
 * тем, что игрок на самом деле сделал, и переживает любую перезагрузку. Счётчик
 * `tutorialStep` этой роли не несёт и нести не может: он застревает на `TOILET` и
 * на ключе, и на двери.
 *
 * Держится всё на четырёх опознавательных знаках, и объявлены они здесь — одним
 * куском, ниже обоих слоёв, которые их читают. Генератор (`gen/living/tutorial_start.ts`)
 * их СТАВИТ, интерфейс (`systems/target_guide.ts`) их ИЩЕТ. Раньше эти четыре
 * знака лежали двумя копиями в разных слоях, и связь сиквенса с интерфейсом
 * приходилось восстанавливать чтением обоих.
 *
 * Практический смысл для тех, кто добавляет контент: в стартовых комнатах можно
 * менять всё — размеры, место, обстановку, соседей, — кроме этих четырёх знаков.
 * Замок — `tests/tutorial-start-contract.test.ts` и `tests/tutorial-guide-stages.test.ts`.
 */

export const TUTORIAL_START = {
  /** Тег обеих стартовых комнат: по нему ищутся раковина и уборная. */
  roomTag: 'tutorial',
  /** Ключ из Уборной. Он же назван в `keyId` запертой двери из Столовой. */
  keyId: 'tut_cafe_key',
  /** Кто принимает игрока за дверью: его реплики и учат управлению. */
  tutorNpcPackageId: 'olga',
  /** Нужда выше этого ещё читается срочной, и текущим шагом остаётся уборная. */
  toiletUrge: 15,
} as const;
